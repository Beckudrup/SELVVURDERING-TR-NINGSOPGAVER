public boolean sum28(int[] nums) {
  
    int sum = 0;
    boolean n = false;
    
    for(int i = 0;i<nums.length;i++){
      if(nums[i]==2){
  
      sum = sum + nums[i];
    }
    }

    if(sum == 8){
    n = true;
    }
return n;
}

  
  
